apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: radixalerts.radix.equinor.com
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.2
  labels:
  {{- include "testchart2.labels" . | nindent 4 }}
spec:
  group: radix.equinor.com
  names:
    kind: RadixAlert
    listKind: RadixAlertList
    plural: radixalerts
    singular: radixalert
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: RadixAlert describe alert config for a radix environment
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RadixAlertSpec is the spec for a RadixAlert
            properties:
              alerts:
                items:
                  properties:
                    alert:
                      type: string
                    receiver:
                      type: string
                  required:
                  - alert
                  - receiver
                  type: object
                type: array
              receivers:
                additionalProperties:
                  properties:
                    slackConfig:
                      properties:
                        enabled:
                          type: boolean
                      required:
                      - enabled
                      type: object
                  required:
                  - slackConfig
                  type: object
                type: object
            required:
            - alerts
            - receivers
            type: object
          status:
            description: RadixAlertStatus is the status for a RadixAlert
            properties:
              reconciled:
                format: date-time
                type: string
            required:
            - reconciled
            type: object
        required:
        - spec
        - status
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
