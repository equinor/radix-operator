---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: radixalerts.radix.equinor.com
spec:
  group: radix.equinor.com
  names:
    kind: RadixAlert
    listKind: RadixAlertList
    plural: radixalerts
    shortNames:
    - ral
    singular: radixalert
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: RadixAlert describes configuration for setting up alerts for
          environments or pipeline jobs
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec is the desired state of the RadixAlert
            properties:
              alerts:
                items:
                  description: Alert defines the mapping between an alert name and
                    its receiver
                  properties:
                    alert:
                      description: Alert is the name of the alert to configure
                      type: string
                    receiver:
                      description: Receiver is the name of the receiver that should
                        handle this alert
                      type: string
                  required:
                  - alert
                  - receiver
                  type: object
                type: array
              receivers:
                additionalProperties:
                  description: Receiver defines the configuration for an alert receiver
                  properties:
                    slackConfig:
                      description: SlackConfig contains the Slack-specific configuration
                        for this receiver
                      properties:
                        enabled:
                          description: Enabled indicates whether Slack notifications
                            are enabled for this receiver
                          type: boolean
                      required:
                      - enabled
                      type: object
                  required:
                  - slackConfig
                  type: object
                type: object
            required:
            - alerts
            - receivers
            type: object
          status:
            description: Status is the observed state of the RadixAlert
            properties:
              message:
                description: Message provides additional information about the reconciliation
                  state, typically error details when reconciliation fails
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation observed by the
                  controller
                format: int64
                type: integer
              reconcileStatus:
                description: ReconcileStatus indicates whether the last reconciliation
                  succeeded or failed
                type: string
              reconciled:
                description: Reconciled is the timestamp of the last successful reconciliation
                format: date-time
                type: string
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
