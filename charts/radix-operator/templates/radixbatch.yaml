apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: radixbatches.radix.equinor.com
spec:
  group: radix.equinor.com
  names:
    kind: RadixBatch
    listKind: RadixBatchList
    plural: radixbatches
    shortNames:
    - rb
    singular: radixbatch
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.radixDeploymentJobRef.job
      name: Job
      type: string
    - jsonPath: .status.condition.type
      name: Condition
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: RadixBatch describes a job
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              jobs:
                items:
                  description: RadixBatchJob defines the spec for a single job in
                    the batch
                  properties:
                    jobId:
                      type: string
                    name:
                      type: string
                    node:
                      description: RadixNode defines node attributes, where container
                        should be scheduled
                      properties:
                        gpu:
                          description: Gpu Optional. Holds lists of node GPU types,
                            with dashed types to exclude
                          type: string
                        gpuCount:
                          description: GpuCount Optional. Holds minimum count of GPU
                            on node
                          type: string
                      required:
                      - gpu
                      - gpuCount
                      type: object
                    payloadSecretRef:
                      description: PayloadSecretKeySelector selects a Secret to get
                        job payload from
                      properties:
                        key:
                          description: The key of the entry in the Secret resource's
                            `data` field to be used.
                          type: string
                        name:
                          description: 'Name of the resource being referred to. More
                            info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                          type: string
                      required:
                      - key
                      - name
                      type: object
                    resources:
                      description: ResourceRequirements describes the compute resource
                        requirements.
                      properties:
                        limits:
                          additionalProperties:
                            type: string
                          description: 'Limits describes the maximum amount of compute
                            resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                          type: object
                        requests:
                          additionalProperties:
                            type: string
                          description: 'Requests describes the minimum amount of compute
                            resources required. If Requests is omitted for a container,
                            it defaults to Limits if that is explicitly specified,
                            otherwise to an implementation-defined value. More info:
                            https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                          type: object
                      type: object
                    stop:
                      type: boolean
                    tags:
                      additionalProperties:
                        type: string
                      type: object
                    timeLimitSeconds:
                      format: int64
                      type: integer
                  required:
                  - name
                  type: object
                maxItems: 100
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              radixDeploymentJobRef:
                description: A reference to a specific job within a RadixDeployment
                  resource.
                properties:
                  job:
                    description: The name of the job in the RadixDeployment's `jobs`
                      list to be used
                    type: string
                  name:
                    description: 'Name of the resource being referred to. More info:
                      https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                    type: string
                required:
                - job
                - name
                type: object
            required:
            - jobs
            - radixDeploymentJobRef
            type: object
          status:
            description: RadixBatchStatus is the status for a RadixBatch
            properties:
              condition:
                description: The batch is completed when all jobs are in a completed
                  phase (Succeeded, Failed or Stopped)
                properties:
                  message:
                    type: string
                  reason:
                    type: string
                  type:
                    enum:
                    - Waiting
                    - Running
                    - Running
                    - Completed
                    type: string
                required:
                - type
                type: object
              jobStatuses:
                description: Status for each job defined in spec.jobs
                items:
                  description: RadixBatchJobStatus is the status for a specific item
                    in spec.items
                  properties:
                    created:
                      format: date-time
                      type: string
                    ended:
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about
                        why the job is in this phase
                      type: string
                    name:
                      type: string
                    phase:
                      description: RadixBatchJobPhase represents the phase of the
                        job
                      enum:
                      - Waiting
                      - Running
                      - Succeeded
                      - Failed
                      - Stopped
                      type: string
                    reason:
                      description: A brief CamelCase message indicating details about
                        why the job is in this phase
                      type: string
                    started:
                      format: date-time
                      type: string
                  required:
                  - name
                  type: object
                type: array
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}