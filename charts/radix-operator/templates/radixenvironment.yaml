---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: radixenvironments.radix.equinor.com
spec:
  group: radix.equinor.com
  names:
    kind: RadixEnvironment
    listKind: RadixEnvironmentList
    plural: radixenvironments
    shortNames:
    - re
    singular: radixenvironment
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: RadixEnvironment is a Custom Resource Definition
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec is the desired state of the RadixEnvironment
            properties:
              appName:
                type: string
              egress:
                description: Egress defines egress rules
                properties:
                  allowRadix:
                    description: Allow or deny outgoing traffic to the public IP of
                      the Radix cluster.
                    type: boolean
                  rules:
                    description: Defines a list of egress rules.
                    items:
                      description: EgressRule defines an egress rule.
                      properties:
                        destinations:
                          description: |-
                            List of allowed destinations.
                            Each destination must be a valid IPv4 CIDR.
                          items:
                            pattern: ^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)(\/([0-9]|[1-2][0-9]|3[0-2]))?$
                            type: string
                          minItems: 1
                          type: array
                        ports:
                          description: List of allowed ports.
                          items:
                            description: EgressPort defines a port in context of EgressRule.
                            properties:
                              port:
                                description: Port number.
                                format: int32
                                maximum: 65535
                                minimum: 1
                                type: integer
                              protocol:
                                description: Protocol.
                                enum:
                                - TCP
                                - UDP
                                type: string
                            required:
                            - port
                            - protocol
                            type: object
                          minItems: 1
                          type: array
                      required:
                      - destinations
                      - ports
                      type: object
                    maxItems: 1000
                    type: array
                type: object
              envName:
                type: string
                x-kubernetes-validations:
                - message: envName must not be empty or 'app'
                  rule: self != '' && self != 'app'
            required:
            - appName
            - envName
            type: object
          status:
            description: Status is the observed state of the RadixEnvironment
            properties:
              message:
                description: Message provides additional information about the reconciliation
                  state, typically error details when reconciliation fails
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation observed by the
                  controller
                format: int64
                type: integer
              orphaned:
                description: Orphaned is true when this environment was removed from
                  the RadixApplication
                type: boolean
              orphanedTimestamp:
                description: OrphanedTimestamp is a timestamp representing the server
                  time when this RadixEnvironment was removed from the RadixApplication
                format: date-time
                type: string
              reconcileStatus:
                description: ReconcileStatus indicates whether the last reconciliation
                  succeeded or failed
                type: string
              reconciled:
                description: Reconciled is the timestamp of the last successful reconciliation
                format: date-time
                type: string
            required:
            - orphaned
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
