apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: radixscheduledjobs.radix.equinor.com
spec:
  group: radix.equinor.com
  names:
    kind: RadixScheduledJob
    listKind: RadixScheduledJobList
    plural: radixscheduledjobs
    shortNames:
    - rsj
    singular: radixscheduledjob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.radixDeploymentJobRef.job
      name: Job
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: RadixScheduledJob describes a job
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: RadixScheduledJobSpec is the spec for a RadixScheduledJob
            properties:
              bool:
                type: boolean
              jobId:
                type: string
              node:
                description: RadixNode defines node attributes, where container should
                  be scheduled
                properties:
                  gpu:
                    description: Gpu Optional. Holds lists of node GPU types, with
                      dashed types to exclude
                    type: string
                  gpuCount:
                    description: GpuCount Optional. Holds minimum count of GPU on
                      node
                    type: string
                required:
                - gpu
                - gpuCount
                type: object
              payloadSecretRef:
                description: PayloadSecretKeySelector selects a Secret to get job
                  payload from
                properties:
                  key:
                    description: The key of the entry in the Secret resource's `data`
                      field to be used.
                    type: string
                  name:
                    description: 'Name of the resource being referred to. More info:
                      https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                    type: string
                required:
                - key
                - name
                type: object
              radixDeploymentJobRef:
                description: A reference to a specific job within a RadixDeployment
                  resource.
                properties:
                  job:
                    description: The name of the job in the RadixDeployment's `jobs`
                      list to be used
                    type: string
                  name:
                    description: 'Name of the resource being referred to. More info:
                      https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                    type: string
                required:
                - job
                - name
                type: object
              resources:
                description: ResourceRequirements describes the compute resource requirements.
                properties:
                  limits:
                    additionalProperties:
                      type: string
                    description: 'Limits describes the maximum amount of compute resources
                      allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                    type: object
                  requests:
                    additionalProperties:
                      type: string
                    description: 'Requests describes the minimum amount of compute
                      resources required. If Requests is omitted for a container,
                      it defaults to Limits if that is explicitly specified, otherwise
                      to an implementation-defined value. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                    type: object
                type: object
              timeLimitSeconds:
                format: int64
                type: integer
            required:
            - radixDeploymentJobRef
            type: object
          status:
            description: RadixScheduledJobStatus is the status for a RadixScheduledJob
            properties:
              created:
                format: date-time
                type: string
              ended:
                format: date-time
                type: string
              phase:
                description: RadixScheduledJobPhase represents the phase of the job
                enum:
                - Waiting
                - Running
                - Succeeded
                - Failed
                - Stopped
                type: string
              reason:
                description: The reason why the job is in its current phase
                type: string
              started:
                format: date-time
                type: string
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}