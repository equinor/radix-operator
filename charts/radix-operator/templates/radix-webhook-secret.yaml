kind: Secret
apiVersion: v1
metadata:
  name: {{ include "radix-webhook-certs.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "radix-webhook.labels" . | nindent 4 }}
type: Opaque
