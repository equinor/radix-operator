---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.2
  name: radixenvironments.radix.equinor.com
spec:
  group: radix.equinor.com
  names:
    kind: RadixEnvironment
    listKind: RadixEnvironmentList
    plural: radixenvironments
    singular: radixenvironment
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: RadixEnvironment is a Custom Resource Definition
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RadixEnvironmentSpec is the spec for an RE
            properties:
              appName:
                type: string
              egress:
                description: EgressConfig contains egress configuration.
                properties:
                  allowRadix:
                    description: Allow or deny outgoing traffic to the public IP of
                      the Radix cluster.
                    type: boolean
                  rules:
                    description: Defines a list of egress rules.
                    items:
                      description: EgressRule defines an egress rule.
                      properties:
                        destinations:
                          description: |-
                            List of allowed destinations.
                            Each destination must be a valid IPv4 CIDR.
                          items:
                            pattern: ^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)(\/([0-9]|[1-2][0-9]|3[0-2]))?$
                            type: string
                          minItems: 1
                          type: array
                        ports:
                          description: List of allowed ports.
                          items:
                            description: EgressPort defines a port in context of EgressRule.
                            properties:
                              port:
                                description: Port number.
                                format: int32
                                maximum: 65535
                                minimum: 1
                                type: integer
                              protocol:
                                description: Protocol.
                                enum:
                                - TCP
                                - UDP
                                type: string
                            required:
                            - port
                            - protocol
                            type: object
                          minItems: 1
                          type: array
                      required:
                      - destinations
                      - ports
                      type: object
                    maxItems: 1000
                    type: array
                type: object
              envName:
                type: string
            required:
            - appName
            - envName
            type: object
          status:
            description: RadixEnvironmentStatus is the status for an RE
            properties:
              orphaned:
                type: boolean
              orphanedTimestamp:
                description: OrphanedTimestamp is a timestamp representing the server
                  time when this RadixEnvironment was removed from the RadixApplication
                format: date-time
                type: string
              reconciled:
                format: date-time
                type: string
            required:
            - orphaned
            - reconciled
            type: object
        required:
        - spec
        - status
        type: object
    served: true
    storage: true
